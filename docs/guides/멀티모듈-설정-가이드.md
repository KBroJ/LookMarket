# 멀티 모듈 설정 가이드

> LookMarket 프로젝트의 멀티 모듈 Gradle 설정 및 실행 방법

**날짜**: 2025-12-16

---

## 📋 목차

1. [프로젝트 구조](#프로젝트-구조)
2. [Gradle 설정](#gradle-설정)
3. [모듈 간 연결 방식](#모듈-간-연결-방식)
4. [코드 작성 예시](#코드-작성-예시)
5. [실제 동작 예시](#실제-동작-예시)
6. [빌드 및 실행](#빌드-및-실행)
7. [빌드 결과물](#빌드-결과물)
8. [트러블슈팅](#트러블슈팅)

---

## 프로젝트 구조

### LookMarket 멀티 모듈 구조

```
lookmarket/
├── settings.gradle              # 모듈 등록
├── build.gradle                 # 공통 설정 (Java 21, Lombok 등)
│
├── lookmarket-domain/           # 모듈 1: 도메인
│   ├── build.gradle             # 의존성: common만
│   └── src/main/java/
│       └── com/lookmarket/domain/
│           ├── user/
│           │   ├── User.java
│           │   └── UserRepository.java (인터페이스)
│           ├── order/
│           └── product/
│
├── lookmarket-infrastructure/   # 모듈 2: 인프라
│   ├── build.gradle             # 의존성: common, domain, JPA, Kafka, Redis
│   └── src/main/java/
│       └── com/lookmarket/infrastructure/
│           ├── user/
│           │   ├── UserEntity.java
│           │   ├── JpaUserRepository.java
│           │   └── UserAdapter.java
│           ├── kafka/
│           └── redis/
│
├── lookmarket-application/      # 모듈 3: 애플리케이션 서비스
│   ├── build.gradle             # 의존성: common, domain
│   └── src/main/java/
│       └── com/lookmarket/application/
│           ├── user/
│           │   └── UserService.java
│           └── order/
│
├── lookmarket-api/              # 모듈 4: API (실행 가능)
│   ├── build.gradle             # 의존성: 모든 모듈
│   └── src/main/java/
│       └── com/lookmarket/api/
│           ├── LookMarketApplication.java  # @SpringBootApplication
│           └── user/
│               └── UserController.java
│
└── lookmarket-common/           # 모듈 5: 공통
    ├── build.gradle             # 의존성: 없음 (최하위)
    └── src/main/java/
        └── com/lookmarket/common/
            └── util/
```

---

## Gradle 설정

### Step 1: settings.gradle에 모듈 등록

```gradle
// settings.gradle
rootProject.name = 'lookmarket'

include 'lookmarket-api'
include 'lookmarket-application'
include 'lookmarket-domain'
include 'lookmarket-infrastructure'
include 'lookmarket-common'
```

**역할**: Gradle에게 "이 5개 모듈이 하나의 프로젝트에 속한다"고 알림

### Step 2: 루트 build.gradle (공통 설정)

```gradle
// build.gradle (루트)
plugins {
    id 'java'
    id 'org.springframework.boot' version '3.3.7' apply false
    id 'io.spring.dependency-management' version '1.1.7' apply false
}

// 모든 서브 프로젝트에 공통 적용
subprojects {
    apply plugin: 'java'
    apply plugin: 'io.spring.dependency-management'

    group = 'com.lookmarket'
    version = '0.0.1-SNAPSHOT'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }

    repositories {
        mavenCentral()
    }

    dependencies {
        // 모든 모듈에 공통으로 적용되는 의존성
        compileOnly 'org.projectlombok:lombok'
        annotationProcessor 'org.projectlombok:lombok'
        testImplementation 'org.springframework.boot:spring-boot-starter-test'
    }
}
```

### Step 3: 각 모듈의 build.gradle

#### lookmarket-domain/build.gradle

```gradle
dependencies {
    implementation project(':lookmarket-common')
    // infrastructure, api는 의존성에 없음!
}
```

**포인트**: Domain은 **순수하게 common만 의존**

#### lookmarket-infrastructure/build.gradle

```gradle
dependencies {
    implementation project(':lookmarket-common')
    implementation project(':lookmarket-domain')  // Domain 참조 가능

    // 인프라 라이브러리
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.kafka:spring-kafka'
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
    implementation 'org.springframework.data:spring-data-elasticsearch'
    implementation 'com.querydsl:querydsl-jpa:5.1.0:jakarta'
    annotationProcessor 'com.querydsl:querydsl-apt:5.1.0:jakarta'
    annotationProcessor 'jakarta.annotation:jakarta.annotation-api'
    annotationProcessor 'jakarta.persistence:jakarta.persistence-api'

    // 데이터베이스
    runtimeOnly 'com.mysql:mysql-connector-j'
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.flywaydb:flyway-mysql'
}
```

**포인트**: Infrastructure는 **Domain + 외부 라이브러리(JPA, Kafka 등)**

#### lookmarket-application/build.gradle

```gradle
dependencies {
    implementation project(':lookmarket-common')
    implementation project(':lookmarket-domain')

    implementation 'org.springframework.boot:spring-boot-starter'
    implementation 'org.springframework:spring-tx'
}
```

**포인트**: Application은 **Domain만 참조** (Infrastructure 의존 없음)

#### lookmarket-api/build.gradle

```gradle
plugins {
    id 'org.springframework.boot'
}

dependencies {
    // 모든 모듈 의존
    implementation project(':lookmarket-common')
    implementation project(':lookmarket-domain')
    implementation project(':lookmarket-application')
    implementation project(':lookmarket-infrastructure')

    // API 레이어 라이브러리
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    implementation 'io.jsonwebtoken:jjwt-api:0.12.6'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.12.6'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.12.6'
}
```

**포인트**: API는 **최상위 레이어로 모든 모듈 참조 가능**

#### lookmarket-common/build.gradle

```gradle
dependencies {
    // 외부 의존성 없음 (최하위 모듈)
}
```

**포인트**: Common은 **다른 모듈에 의존하지 않음**

---

## 모듈 간 연결 방식

### 의존성 그래프

```
lookmarket-api
 ├─ lookmarket-application
 │   └─ lookmarket-domain
 │       └─ lookmarket-common
 ├─ lookmarket-infrastructure
 │   ├─ lookmarket-domain
 │   │   └─ lookmarket-common
 │   └─ lookmarket-common
 └─ lookmarket-common
```

### 의존성 규칙

| 모듈 | 의존 가능한 모듈 | 설명 |
|------|----------------|------|
| **common** | 없음 | 최하위 모듈, 다른 모듈에 의존하지 않음 |
| **domain** | common | 순수 비즈니스 로직, 프레임워크 독립 |
| **infrastructure** | common, domain | Domain의 포트(인터페이스) 구현 |
| **application** | common, domain | 유즈케이스 오케스트레이션 |
| **api** | 모든 모듈 | 최상위 레이어, 실행 가능한 애플리케이션 |

---

## 코드 작성 예시

### 1. Domain Layer (포트 정의)

```java
// lookmarket-domain/src/main/java/.../User.java
package com.lookmarket.domain.user;

public class User {
    private Long id;
    private String email;
    private String password;

    // 순수 비즈니스 로직만
    public void changeEmail(String newEmail) {
        validateEmail(newEmail);
        this.email = newEmail;
    }

    private void validateEmail(String email) {
        if (email == null || !email.contains("@")) {
            throw new IllegalArgumentException("Invalid email");
        }
    }
}

// lookmarket-domain/src/main/java/.../UserRepository.java (포트)
package com.lookmarket.domain.user;

public interface UserRepository {
    User save(User user);
    Optional<User> findById(Long id);
    Optional<User> findByEmail(String email);
}
```

**포인트**: Domain은 **인터페이스(포트)만 정의**, 구현은 Infrastructure에서

### 2. Infrastructure Layer (어댑터 구현)

```java
// lookmarket-infrastructure/.../UserEntity.java
package com.lookmarket.infrastructure.user;

import com.lookmarket.domain.user.User;  // ✅ domain 모듈 참조 가능!

@Entity
@Table(name = "users")
public class UserEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String email;
    private String password;

    // Domain ↔ Entity 변환
    public static UserEntity fromDomain(User user) {
        return new UserEntity(user.getId(), user.getEmail(), ...);
    }

    public User toDomain() {
        return User.reconstitute(this.id, this.email, ...);
    }
}

// lookmarket-infrastructure/.../JpaUserRepository.java
package com.lookmarket.infrastructure.user;

@Repository
public interface JpaUserRepository extends JpaRepository<UserEntity, Long> {
    Optional<UserEntity> findByEmail(String email);
}

// lookmarket-infrastructure/.../UserAdapter.java
package com.lookmarket.infrastructure.user;

import com.lookmarket.domain.user.User;
import com.lookmarket.domain.user.UserRepository;  // 포트 구현

@Component
public class UserAdapter implements UserRepository {
    private final JpaUserRepository jpaUserRepository;

    @Override
    public User save(User user) {
        UserEntity entity = UserEntity.fromDomain(user);
        UserEntity saved = jpaUserRepository.save(entity);
        return saved.toDomain();
    }

    @Override
    public Optional<User> findById(Long id) {
        return jpaUserRepository.findById(id)
            .map(UserEntity::toDomain);
    }
}
```

**포인트**: Infrastructure는 **Domain의 포트를 구현**하고, Domain ↔ JPA Entity 변환

### 3. Application Layer

```java
// lookmarket-application/.../UserService.java
package com.lookmarket.application.user;

import com.lookmarket.domain.user.User;
import com.lookmarket.domain.user.UserRepository;

@Service
@Transactional
public class UserService {
    private final UserRepository userRepository;

    public User createUser(String email, String password) {
        // 중복 체크
        if (userRepository.findByEmail(email).isPresent()) {
            throw new IllegalArgumentException("Email already exists");
        }

        // Domain 로직 호출
        User user = User.create(email, password, ...);

        // 저장
        return userRepository.save(user);
    }
}
```

**포인트**: Application은 **Domain 객체만 다룸** (Infrastructure 의존 없음)

### 4. API Layer

```java
// lookmarket-api/.../UserController.java
package com.lookmarket.api.user;

import com.lookmarket.domain.user.User;
import com.lookmarket.application.user.UserService;

@RestController
@RequestMapping("/api/v1/users")
public class UserController {
    private final UserService userService;

    @PostMapping
    public ResponseEntity<UserResponse> createUser(@RequestBody UserRequest request) {
        User user = userService.createUser(request.getEmail(), request.getPassword());
        return ResponseEntity.ok(UserResponse.from(user));
    }
}

// DTO 변환
record UserResponse(Long id, String email) {
    static UserResponse from(User user) {
        return new UserResponse(user.getId(), user.getEmail());
    }
}
```

**포인트**: API는 **모든 모듈 참조 가능**, DTO 변환은 API 레이어에서

---

## 실제 동작 예시

### 예시 1: Domain에서 Infrastructure 참조 시도 (실패)

```java
// lookmarket-domain/.../Order.java
package com.lookmarket.domain.order;

import com.lookmarket.infrastructure.order.JpaOrderRepository;
// ❌ 컴파일 에러!
// error: package com.lookmarket.infrastructure.order does not exist
```

**에러 메시지**:
```
Compilation failure: package com.lookmarket.infrastructure.order does not exist
```

**이유**: `lookmarket-domain/build.gradle`에 infrastructure 의존성이 없음

### 예시 2: Infrastructure에서 Domain 참조 (성공)

```java
// lookmarket-infrastructure/.../UserAdapter.java
package com.lookmarket.infrastructure.user;

import com.lookmarket.domain.user.User;  // ✅ 정상 동작!
import com.lookmarket.domain.user.UserRepository;

@Component
public class UserAdapter implements UserRepository {
    // 정상 동작
}
```

**이유**: `lookmarket-infrastructure/build.gradle`에 domain 의존성 있음

### 예시 3: API에서 모든 모듈 참조 (성공)

```java
// lookmarket-api/.../UserController.java
package com.lookmarket.api.user;

import com.lookmarket.domain.user.User;  // ✅
import com.lookmarket.application.user.UserService;  // ✅
import com.lookmarket.infrastructure.user.UserAdapter;  // ✅

@RestController
public class UserController {
    // 모두 정상 동작!
}
```

**이유**: api 모듈은 최상위 레이어로 모든 모듈 의존성 보유

---

## 빌드 및 실행

### 전체 빌드

```bash
# 루트에서 전체 빌드
./gradlew clean build

# 테스트 제외하고 빌드
./gradlew clean build -x test
```

### 모듈별 빌드

```bash
# 특정 모듈만 빌드
./gradlew :lookmarket-domain:build
./gradlew :lookmarket-infrastructure:build
./gradlew :lookmarket-api:build
```

### 모듈별 테스트

```bash
# Domain 모듈만 테스트 (빠름, 순수 로직만)
./gradlew :lookmarket-domain:test

# Infrastructure 모듈 테스트 (Testcontainers 사용)
./gradlew :lookmarket-infrastructure:test

# API 모듈 테스트 (E2E)
./gradlew :lookmarket-api:test

# 전체 테스트
./gradlew test
```

### 애플리케이션 실행

```bash
# API 모듈 실행 (Spring Boot Application)
./gradlew :lookmarket-api:bootRun

# 또는 JAR 파일로 실행
java -jar lookmarket-api/build/libs/lookmarket-api-0.0.1-SNAPSHOT.jar
```

---

## 빌드 결과물

### 각 모듈별 JAR 파일 생성

```
lookmarket/
├── lookmarket-domain/build/libs/
│   └── lookmarket-domain-0.0.1-SNAPSHOT.jar
├── lookmarket-infrastructure/build/libs/
│   └── lookmarket-infrastructure-0.0.1-SNAPSHOT.jar
├── lookmarket-application/build/libs/
│   └── lookmarket-application-0.0.1-SNAPSHOT.jar
├── lookmarket-api/build/libs/
│   └── lookmarket-api-0.0.1-SNAPSHOT.jar  (실행 가능한 JAR)
└── lookmarket-common/build/libs/
    └── lookmarket-common-0.0.1-SNAPSHOT.jar
```

**실행 가능한 JAR**: `lookmarket-api`만 실행 가능 (@SpringBootApplication 포함)

---

## 트러블슈팅

### 문제 1: "cannot find symbol" 에러

```
error: cannot find symbol
import com.lookmarket.domain.user.User;
```

**원인**: 해당 모듈의 `build.gradle`에 의존성이 없음

**해결**:
```gradle
// build.gradle에 추가
dependencies {
    implementation project(':lookmarket-domain')
}
```

### 문제 2: 순환 의존성

```
Circular dependency between the following projects:
:lookmarket-domain -> :lookmarket-infrastructure -> :lookmarket-domain
```

**원인**: Domain과 Infrastructure가 서로 참조

**해결**: Hexagonal Architecture 원칙 준수
- Domain은 **인터페이스만 정의**
- Infrastructure는 **Domain을 참조만** (역참조 금지)

### 문제 3: 빌드 순서 문제

**증상**: Infrastructure 빌드 시 Domain 클래스를 찾지 못함

**해결**: Gradle이 자동으로 의존성 순서 해결
```bash
# 전체 빌드 시 자동으로 domain → infrastructure 순서로 빌드됨
./gradlew build
```

### 문제 4: IDE에서 모듈 인식 안 됨

**IntelliJ IDEA**:
1. File → Project Structure → Modules
2. "+" 버튼으로 각 모듈 수동 추가
3. 또는 Gradle 탭에서 "Reload All Gradle Projects"

**VSCode**:
1. Java Extension Pack 설치
2. `Ctrl + Shift + P` → "Java: Clean Java Language Server Workspace"
3. VSCode 재시작

---

## 다음 단계

### 관련 문서
- [멀티모듈 vs 폴더구분 비교분석](../learning/멀티모듈-vs-폴더구분-비교분석.md) - 왜 멀티 모듈을 선택했는가?
- [Hexagonal Architecture](../../architecture/ENFORCEMENT_RULES.md) - 아키텍처 강제 규칙
- [CLAUDE.md](../../CLAUDE.md) - 프로젝트 전체 가이드

### 추가 학습
- [Gradle Multi-Project Builds 공식 문서](https://docs.gradle.org/current/userguide/multi_project_builds.html)
- [Spring Boot Multi-Module Project](https://spring.io/guides/gs/multi-module/)

---

**마지막 업데이트**: 2025-12-16
**관리자**: LookMarket 개발팀
