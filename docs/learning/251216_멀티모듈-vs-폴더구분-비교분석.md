# 멀티 모듈 vs 폴더 구분 비교 분석

> **작성일시**: 2025-12-16 18:00
>
> 프로젝트 구조 방식의 차이점과 LookMarket이 멀티 모듈을 선택한 이유

**카테고리**: 아키텍처

---

## 📋 목차

1. [멀티 모듈이란?](#멀티-모듈이란)
2. [폴더 구분 vs 멀티 모듈](#폴더-구분-vs-멀티-모듈)
3. [핵심 차이: 의존성 제어](#핵심-차이-의존성-제어)
4. [왜 멀티 모듈을 선택했나?](#왜-멀티-모듈을-선택했나)
5. [장단점 비교](#장단점-비교)
6. [다른 프로젝트 예시](#다른-프로젝트-예시)
7. [비교 표](#비교-표)
8. [결론](#결론)

---

## 멀티 모듈이란?

**멀티 모듈(Multi Module)**은 하나의 프로젝트를 여러 개의 독립적인 서브 프로젝트(모듈)로 나누는 구조입니다.

### 일반적인 오해

"패키지나 폴더로 계층을 나누는 것과 같다" → **❌ 다릅니다!**

멀티 모듈은 각 모듈이 **독립적인 Gradle 프로젝트**로, 별도의 빌드 설정과 의존성을 가집니다.

---

## 폴더 구분 vs 멀티 모듈

### 방식 1: 일반적인 폴더(패키지) 구분 (Single Module)

```
lookmarket/
├── build.gradle  (하나의 빌드 파일)
└── src/
    └── main/
        └── java/
            └── com/
                └── lookmarket/
                    ├── domain/          # 도메인
                    ├── application/     # 서비스
                    ├── infrastructure/  # 인프라
                    ├── api/            # 컨트롤러
                    └── common/         # 유틸리티
```

**특징**:
- ✅ 하나의 프로젝트, 하나의 JAR 파일
- ✅ 설정이 간단함
- ❌ 모든 코드가 같은 빌드 사이클
- ❌ 패키지로만 구분, **의존성 제어가 불가능**

**문제점**:
```java
// ❌ domain 패키지에서 infrastructure를 직접 참조 가능 (나쁜 설계!)
package com.lookmarket.domain;

import com.lookmarket.infrastructure.JpaUserRepository; // 막을 수 없음!

public class User {
    public void save() {
        JpaUserRepository repo = new JpaUserRepository(); // 😱
        repo.save(this);
    }
}
```

### 방식 2: 멀티 모듈 (Multi Module) - LookMarket 프로젝트

```
lookmarket/
├── settings.gradle           # 모듈 등록
├── build.gradle              # 공통 설정
├── lookmarket-domain/        # 독립적인 Gradle 프로젝트
│   └── build.gradle          # domain 전용 빌드 파일
├── lookmarket-application/   # 독립적인 Gradle 프로젝트
│   └── build.gradle
├── lookmarket-infrastructure/# 독립적인 Gradle 프로젝트
│   └── build.gradle
├── lookmarket-api/          # 독립적인 Gradle 프로젝트
│   └── build.gradle
└── lookmarket-common/       # 독립적인 Gradle 프로젝트
    └── build.gradle
```

**특징**:
- ✅ 각 모듈이 독립적인 프로젝트
- ✅ 각 모듈마다 별도의 `build.gradle`
- ✅ **의존성을 강제로 제어 가능**
- ✅ 각 모듈마다 별도의 JAR 파일 생성
- ❌ 초기 설정이 복잡함

---

## 핵심 차이: 의존성 제어

### 폴더 구분 방식의 문제점

```java
// ❌ 폴더 구분 방식에서는 이런 나쁜 코드를 막을 수 없음!

// domain 패키지에서 infrastructure를 직접 참조 (위반!)
package com.lookmarket.domain;

import com.lookmarket.infrastructure.JpaUserRepository; // 가능해버림!

public class User {
    public void save() {
        JpaUserRepository repo = new JpaUserRepository(); // 😱
        repo.save(this);
    }
}
```

**문제**: Domain이 Infrastructure에 의존하면 안 되는데, **컴파일 타임에 막을 방법이 없음**

### 멀티 모듈 방식의 해결

```gradle
// lookmarket-domain/build.gradle
dependencies {
    implementation project(':lookmarket-common')
    // infrastructure를 의존성에 추가하지 않음!
}
```

```java
// ❌ 이제 이 코드는 컴파일 에러!
package com.lookmarket.domain;

import com.lookmarket.infrastructure.JpaUserRepository;
// 컴파일 에러: cannot find symbol
// class JpaUserRepository
```

**해결**: 빌드 시스템이 **강제로 의존성을 제어**하여 잘못된 참조를 원천 차단

---

## 왜 멀티 모듈을 선택했나?

### 이유 1: Hexagonal Architecture 강제

**목표**: Domain이 외부(Infrastructure, API)에 의존하지 않도록 강제

```
의존성 방향:
API → Application → Domain ← Infrastructure
                      ↑
                   Common
```

**멀티 모듈 의존성 설정**:

```gradle
// lookmarket-domain/build.gradle
dependencies {
    implementation project(':lookmarket-common')  // OK
    // infrastructure, api는 의존성에 없음 → 참조 불가능!
}

// lookmarket-infrastructure/build.gradle
dependencies {
    implementation project(':lookmarket-common')
    implementation project(':lookmarket-domain')  // Domain의 인터페이스 구현
}

// lookmarket-api/build.gradle
dependencies {
    implementation project(':lookmarket-common')
    implementation project(':lookmarket-domain')
    implementation project(':lookmarket-application')
    implementation project(':lookmarket-infrastructure')
}
```

**의존성 규칙**:
- ✅ Infrastructure → Domain (OK, 포트 구현)
- ✅ Application → Domain (OK, 유즈케이스 조율)
- ✅ API → Application (OK, 컨트롤러에서 서비스 호출)
- ❌ Domain → Infrastructure (불가능, 컴파일 에러!)
- ❌ Domain → API (불가능, 컴파일 에러!)

### 이유 2: 빌드 시간 최적화

```bash
# 폴더 구분 방식
./gradlew build  # 전체를 항상 다시 빌드 (느림)

# 멀티 모듈 방식
./gradlew :lookmarket-domain:build  # domain만 빌드 (빠름)
./gradlew :lookmarket-api:build     # 변경된 모듈만 빌드
```

**예시**:
- Domain 로직만 수정했을 때 → domain 모듈만 재빌드
- Controller만 수정했을 때 → api 모듈만 재빌드
- 큰 프로젝트에서는 **빌드 시간이 몇 분에서 몇 초로 단축**

### 이유 3: 재사용성

```gradle
# domain 모듈을 다른 프로젝트에서 재사용 가능
dependencies {
    implementation 'com.lookmarket:lookmarket-domain:0.0.1'
}
```

**실제 사용 예시**:
- LookMarket 관리자 백오피스 프로젝트 (별도 프로젝트)
- LookMarket Batch 프로젝트 (배치 작업)
- LookMarket Mobile API (모바일 전용 API)

→ 모두 **동일한 domain 모듈 재사용**

### 이유 4: 테스트 격리

```bash
# domain 로직만 순수 테스트 (DB, Kafka 없이 빠르게)
./gradlew :lookmarket-domain:test

# infrastructure 통합 테스트 (Testcontainers 사용)
./gradlew :lookmarket-infrastructure:test

# API E2E 테스트
./gradlew :lookmarket-api:test
```

**장점**:
- Domain 테스트: 초고속 (DB/Kafka/Redis 없이 순수 로직만)
- Infrastructure 테스트: Testcontainers로 격리
- 테스트 실패 시 어느 계층의 문제인지 명확히 파악

### 이유 5: 포트폴리오 가치

**대기업 백엔드 개발자에게 어필**:
- "멀티 모듈 구조 경험" → 대규모 프로젝트 경험으로 인정
- "아키텍처 강제" → 설계 역량 증명
- "실무 수준의 구조" → 취미 프로젝트와 차별화

---

## 장단점 비교

### 폴더 구분 방식

**장점**:
- ✅ 설정이 간단함
- ✅ 작은 프로젝트에 적합
- ✅ 학습 곡선이 낮음
- ✅ 모든 코드가 한 곳에 있어 찾기 쉬움

**단점**:
- ❌ 의존성 제어 불가능 (개발자 규칙에만 의존)
- ❌ 전체를 항상 다시 빌드
- ❌ 계층 위반을 막을 방법이 없음
- ❌ 모듈 재사용 어려움
- ❌ 테스트 격리 어려움

**적합한 경우**:
- 소규모 프로젝트 (1~2명, 1~2개월)
- 프로토타입
- 학습용 토이 프로젝트

### 멀티 모듈 방식

**장점**:
- ✅ **의존성을 강제로 제어** (컴파일 타임에 잘못된 참조 차단)
- ✅ 변경된 모듈만 빌드 (빌드 시간 단축)
- ✅ 모듈 재사용 가능
- ✅ 아키텍처 강제 (Clean Architecture, Hexagonal 등)
- ✅ 테스트 격리 용이
- ✅ 마이크로서비스 전환 용이

**단점**:
- ❌ 초기 설정이 복잡함
- ❌ 학습 곡선이 높음
- ❌ 작은 프로젝트에는 오버엔지니어링
- ❌ Gradle 설정 관리 필요
- ❌ IDE 설정 복잡할 수 있음

**적합한 경우**:
- 중대형 프로젝트
- 팀 프로젝트 (3명 이상)
- 장기 유지보수 예정
- 아키텍처 강제가 필요한 경우
- 포트폴리오 프로젝트 (기술력 어필)

---

## 다른 프로젝트 예시

### 예시 1: Spring Framework 자체도 멀티 모듈!

```
spring-framework/
├── spring-core/       # 핵심 기능
├── spring-beans/      # Bean 관리
├── spring-context/    # ApplicationContext
├── spring-web/        # 웹 기능
├── spring-webmvc/     # Spring MVC
├── spring-data-jpa/   # JPA 지원
└── ...
```

**사용 시**:
```gradle
dependencies {
    implementation 'org.springframework:spring-webmvc:6.1.0'
    // spring-webmvc가 내부적으로 spring-web, spring-context 등을 의존함
}
```

**의존성 체인**:
```
spring-webmvc
  └─ spring-web
      └─ spring-context
          └─ spring-core
```

### 예시 2: 대규모 프로젝트 (Netflix 스타일)

```
netflix-platform/
├── common-domain/     # 공통 도메인 (재사용)
├── common-infra/      # 공통 인프라 (재사용)
├── api-gateway/       # API Gateway
├── user-service/      # User 마이크로서비스
│   ├── user-domain/
│   ├── user-application/
│   └── user-api/
└── order-service/     # Order 마이크로서비스
    ├── order-domain/
    ├── order-application/
    └── order-api/
```

**장점**:
- common-domain을 모든 서비스에서 재사용
- 각 서비스를 독립적으로 배포 가능
- 마이크로서비스로 쉽게 전환

---

## 비교 표

| 비교 항목 | 폴더 구분 | 멀티 모듈 |
|---------|---------|----------|
| **의존성 제어** | ❌ 불가능 (개발자 신뢰) | ✅ 가능 (빌드 시스템 강제) |
| **빌드 시간** | ❌ 항상 전체 빌드 | ✅ 변경된 모듈만 빌드 |
| **모듈 재사용** | ❌ 어려움 | ✅ 쉬움 (Maven/Gradle 배포) |
| **아키텍처 강제** | ❌ 불가능 | ✅ 가능 (컴파일 에러) |
| **테스트 격리** | ❌ 어려움 | ✅ 쉬움 (모듈별 실행) |
| **초기 설정** | ✅ 간단 | ❌ 복잡 |
| **학습 곡선** | ✅ 낮음 | ❌ 높음 |
| **적합한 프로젝트** | 소규모 (1~2명) | 중대형 (3명 이상) |
| **마이크로서비스 전환** | ❌ 어려움 | ✅ 쉬움 |

---

## 결론

### LookMarket 프로젝트에서 멀티 모듈을 선택한 이유:

1. **포트폴리오 목적** - 대기업 백엔드 개발자에게 어필하기 위해 고급 아키텍처 사용
2. **Hexagonal Architecture 강제** - Domain이 Infrastructure에 의존하지 않도록 보장
3. **테스트 격리** - Domain 로직을 DB/Kafka 없이 빠르게 테스트
4. **실무 경험** - 대규모 프로젝트에서 실제로 사용하는 방식
5. **마이크로서비스 준비** - 나중에 모듈을 독립적인 서비스로 분리 용이
6. **기술력 증명** - 단순 CRUD가 아닌 설계 역량 어필

### 언제 멀티 모듈을 사용해야 하나?

**멀티 모듈 추천**:
- ✅ 팀 프로젝트 (3명 이상)
- ✅ 장기 유지보수 예정
- ✅ 아키텍처 강제 필요
- ✅ 포트폴리오 프로젝트 (기술력 어필)
- ✅ 마이크로서비스 전환 계획

**폴더 구분 추천**:
- ✅ 1~2명 소규모 프로젝트
- ✅ 단기 프로토타입
- ✅ 학습용 토이 프로젝트
- ✅ 빠른 개발이 우선

---

## 참고 자료

- [Gradle Multi-Project Builds](https://docs.gradle.org/current/userguide/multi_project_builds.html)
- [Spring Boot Multi-Module Project](https://spring.io/guides/gs/multi-module/)
- [Hexagonal Architecture](https://alistair.cockburn.us/hexagonal-architecture/)
- [Clean Architecture by Robert C. Martin](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)

---

**마지막 업데이트**: 2025-12-16
**관리자**: LookMarket 개발팀
