클로드 코드 단축키 및 사용법

	! : 
		Bash 기능 사용
	ESC 2번 : 
		현재 입력값 모두 제거
	/model : 
		어떤 모델을 사용할 것인지 선택
	★ /resume : 
		클로드 세션이 닫히고 재시작 했을 때 이어서 할 수 있는 기록 리스트
	★ /init : 
		클로드 CLI 가 실행된 폴더를 기준으로 전체적인 프로젝트 구조를 파악하고 CLAUDE.md 파일 생성
	# [저장해야 되는 정보] : 
		입력한 값을 저장하여 쓸수 있도록 함
		
		User Memory : 내 컴퓨터 전체 전체
		Project memory : 현재 프로젝트에만 저장
		
	★ [Alt + m] 또는 [Shift + tab] : 
		Plan Mode 전환
	/clear : 
		새로운 세션 시작
		
	/[command명] [전달변수] : 
		.claude/commands/에 들어간 [Commands명].md을 사용
		
	@[파일경로/파일명] : 
		특정 파일을 지정
		
	/mcp : 
		등록된 mcp 목록 확인
		
	/agents : 
		병렬로 처리할 수 있는 sub_agent 등록 및 관리
		
			Create new agent 
			>
			Project : 현재 프로젝트 내에서만 적용
			Personal : 내 컴퓨터 전체에 적용
			>
			Generate with Claude 
			>
			해당 에이전트 설정 프롬프트 작성
				ex)
					너는 10년 경력의 '클린 코드' 전문가야. 너의 유일한 임무는 React 컴포넌트 파일의 코드를 리팩토링하는 것이야.

					작업 절차:
					1. 지정된 파일을 읽고 분석
					2. SOLID 원칙을 적용하여 구조 개선
					3. 변수명과 함수명을 더 명확하게 변경
					4. 불필요한 중복 코드 제거
					5. 원본 파일을 개선된 코드로 덮어쓰기
					6. 완료되면 'Refactoring complete.' 메시지만 출력
			>
			권한 선택(읽기, 쓰기, 수정 등..)
			> 
			사용 모델 선택
			> 식별 색 선택
			
		※ AI가 자동으로 등록된 agents를 좀 더 활성적으로 쓰게 하기 위해선 
			생성한 agents가 정의된 .md 파일 내용 앞에 
			use PROACTIVELY 또는 MUST BE USED 
			를 붙여주면 좋다고 한다.
	
===============================================================================================================

	◆ claude.md 
		프로젝트가 따라야할 규칙
		
		ex)
			# 개인 블로그 프로젝트 Git 워크플로우 규칙

			## 1. Repository
			- **GitHub Repository:** `[여러분의-GitHub-ID]/my-personal-blog`
			- **Main Branch:** `main`

			## 2. Branching Strategy
			- 모든 기능 개발은 `feature/[이슈번호]-[간단-설명-kebab-case]` 형식의 브랜치에서 진행한다.
			- 이슈 번호가 없는 간단한 수정은 `fix/[간단-설명]` 또는 `chore/[간단-설명]` 브랜치를 사용한다.

			## 3. Commit Message Convention
			- 모든 커밋 메시지는 **Conventional Commits** 명세를 따른다.
			- (예: `feat: Add author profile component`, `fix: Correct typo in footer`)
			- 커밋 본문에는 변경 이유를 명확히 서술하고, 관련된 GitHub 이슈를 `Closes #[이슈번호]` 형식으로 반드시 포함한다.

			## 4. Pull Request (PR) Process
			- 모든 코드는 `main` 브랜치로 직접 푸시할 수 없으며, 반드시 PR을 통해 코드 리뷰를 받아야 한다.
			- PR 제목은 커밋 메시지와 동일한 형식을 따른다.
			- PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md` 템플릿을 사용한다.
			
			
		※ gihub PR 양식
		
			★ github에 존재하는 풀리퀘스트 생성 시 특정 템플릿 형태로 만들도록 강제하는 룰
				프로젝트 경로 .github 폴더 생성 > 하위에 PULL_REQUEST_TEMPLATE.md파일 생성 > 내용작성
		
			파일명 : PULL_REQUEST_TEMPLATE.md

			## 📝 변경사항

			<!-- 이 PR에서 변경된 내용을 간단히 설명해주세요 -->

			### 주요 변경사항
			- [ ] 새로운 블로그 포스트 추가
			- [ ] 기존 포스트 수정
			- [ ] UI/UX 개선
			- [ ] 성능 최적화
			- [ ] 버그 수정
			- [ ] 기타

			### 상세 설명
			<!-- 변경사항에 대한 자세한 설명을 작성해주세요 -->

			## 🔍 변경된 파일

			<!-- 변경된 파일 목록을 작성해주세요 -->
			- `app/blog/posts/` - 새로운 블로그 포스트
			- `app/components/` - 컴포넌트 수정
			- `app/` - 기타 파일 수정

			## 🧪 테스트

			<!-- 테스트 방법을 설명해주세요 -->
			- [ ] 로컬에서 개발 서버 실행 확인
			- [ ] 블로그 포스트 렌더링 확인
			- [ ] 반응형 디자인 확인
			- [ ] SEO 메타데이터 확인

			### 테스트 방법
			```bash
			# 개발 서버 실행
			pnpm dev

			# 빌드 테스트
			pnpm build

			# 린트 검사
			pnpm lint
			```

			## 📸 스크린샷 (선택사항)

			<!-- UI 변경사항이 있는 경우 스크린샷을 첨부해주세요 -->

			## ✅ 체크리스트

			- [ ] 코드가 프로젝트의 코딩 스타일을 따릅니다
			- [ ] 자체적으로 코드를 검토했습니다
			- [ ] 코드에 대한 주석을 작성했습니다 (특히 이해하기 어려운 부분)
			- [ ] 해당 변경사항에 대한 문서를 업데이트했습니다
			- [ ] 새로운 기능에 대한 테스트를 추가했습니다
			- [ ] 모든 테스트가 통과합니다

			## 🚀 배포 관련

			- [ ] 이 변경사항이 프로덕션에 배포되어도 안전합니다
			- [ ] 환경 변수 변경이 필요한 경우 문서화했습니다

			## 📚 관련 이슈

			<!-- 관련된 이슈가 있다면 링크해주세요 -->
			Closes #(이슈 번호)

			## 💡 추가 정보

			<!-- 추가로 전달하고 싶은 정보가 있다면 작성해주세요 -->

			---

			**리뷰어 참고사항:**
			- 블로그 포스트의 경우 마크다운 문법과 내용의 품질을 확인해주세요
			- SEO 최적화가 필요한지 검토해주세요
			- 모바일 반응형이 제대로 작동하는지 확인해주세요

			
			
	◆ Commands 
		반복적인 업무 지시서
		
		* Commands 생성
			프로젝트 루트 경로에 .claude/commands/ 폴더에 [Commands명].md 파일 생성
				ex)
					# 1단계: 먼저 프로젝트의 '설계도'를 읽어서 우리 팀의 디자인 시스템을 숙지한다.
					@claude.md

					# 2단계: 당신의 역할을 정의한다.
					너는 우리 팀의 시니어 프론트엔드 개발자이며, `claude.md`의 규칙을 강박적으로 지키는 사람이야.

					# 3단계: 구체적인 임무를 정의한다.
					# $ARGUMENTS 변수를 사용하여 버튼 텍스트를 동적으로 받는다.
					"$ARGUMENTS" 라는 텍스트를 가진 새로운 React 버튼 JSX 코드를 생성해줘.

					# 4단계: 가장 중요한 지시사항!
					**반드시 `@claude.md` 파일의 "Button Component Rule" 섹션에 명시된 `Required Classes`를 완벽하게 사용해야 해.** 다른 스타일은 절대 추가하지 마.
			
			* $ARGUMENTS : commands 사용시 전달받은 파라미터
			* $SELECTION : 현재 파일의 드래그한 부분에 대해서 명령 및 지시
			
			
	◆ Hooks
		AI의 모든 행동을 감시하고 
		실행 전, 실행 후 등 사전에 정의된 특정 규칙을 토대로 그 행동을 막거나 수정하도록 피드백을 주는 방법
			
			
		Hooks의 종류
			SessionStart : 클로드 세션 시작 시 실행
			PreTollUse : 사전 실행 훅
			PostToolUse : 사후 실행 훅
			UserPromptSubmit : 프롬프트 입력하고 엔터를 누르는 순간 실행
			
		적용방법
			
			1. .claude/settings.json을 생성하고 어떤 hook을 적용할지 설정
			
				* matcher : 어떤 행동 시 hook이 작동할지 지정
					edit : 수정
					create : 생성
					* 로 전체 감시 설정 가능
					
				※ hooks 설정 수정 후 Claude 세션을 재실행 해야 적용된다.
			
				ex)
					{
					  "version": "1.0",
					  "hooks": {
						"SessionStart": [
						  {
							"hooks": [
							  {
								"type": "command", 
								"command": "npm install && echo '✅ 의존성 설치 완료'",
								"timeout": 300
							  }
							]
						  }
						],
						"PreToolUse": [ 
							{ 
								"matcher": "Edit|Write|Create", 
								"hooks": [ 
									{ 
										"type": "command", 
										"command": 
										"bash .claude/hooks/security-check.sh" 
									} 
								] 
							} 
						],
						"PostToolUse": {}
					  }
					}
		
			2. ./claude/hooks/ 경로아래 hooks에서 실행할 스크립트 파일 생성
			
				ex)
					security-check.sh
					
					#!/bin/bash
					# .claude/hooks/security-check.sh

					# Claude로부터 받은 JSON 입력 파싱
					# stdin으로부터 입력을 읽어 파일 경로를 찾습니다.
					INPUT=$(cat)

					# 파일 경로 추출 (tool_input에서 file_path 찾기)
					FILE_PATH=$(echo "$INPUT" | grep -o '"file_path":"[^"]*' | sed 's/"file_path":"//')

					# FILE_PATH가 없으면 환경변수에서 가져오기
					if [ -z "$FILE_PATH" ]; then
						FILE_PATH="$CLAUDE_FILE_PATHS"
					fi

					# 파일 경로가 없으면 통과
					if [ -z "$FILE_PATH" ]; then
						exit 0
					fi

					PATTERNS=(
						"password"
						"api_key" 
						"secret"
						"AWS[A-Z0-9]{16}"
					)

					for pattern in "${PATTERNS[@]}"; do
						# 파일 내용($CLAUDE_TOOL_INPUT)에서 패턴 검사
						if echo "$CLAUDE_TOOL_INPUT" | grep -i -E -q "$pattern"; then
							echo "⚠️ 보안 경고: 민감 정보($pattern)가 코드에 포함되려 합니다! 작업을 차단합니다." >&2
							exit 2 # 2번 코드로 종료하여 Claude 작업을 차단
						fi
						
						# CLAUDE_TOOL_INPUT이 비어있으면 INPUT에서도 검사
						if echo "$INPUT" | grep -i -E -q "$pattern"; then
							echo "⚠️ 보안 경고: 민감 정보($pattern)가 코드에 포함되려 합니다! 작업을 차단합니다." >&2
							exit 2
						fi
					done

					exit 0 # 문제가 없으면 0번 코드로 통과
					
	◆ MCP
		사용자의 자연어 명령어의 의도를 AI가 파악해 알맞은 도구를 사용할 수 있게 하는 것 
		
		★ MCP 초기 환경 설정
		
			# uv 설치 (권장 패키지 매니저)
			
				# macOS/Linux
				curl -LsSf https://astral.sh/uv/install.sh | sh

				# Windows: 
				powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
				
				=> 설치 후 터미널 재실행 필요

			# 가상환경 초기 설정 및 프로젝트 생성 및 이동(선택)
				프로젝트 루트 경로에서 터미널로 
					uv init
				또는
					uv init [생성할 폴더명]
					cd my-mcp-server
			

			# 가상환경 생성 및 활성화
				#생성
				uv venv
				
				# 활성화(Windows)
				.\.venv\Scripts\Activate.ps1

			# MCP 라이브러리 설치
				uv add "mcp[cli]"
				
		★ MCP 등록 및 확인
		
			1. MCP 등록
				명령어 : claude mcp add [별명] [실행할 명령어]
					[별명] : 우리가 부를 이름
					[실행할 명령어] : 실제 서버를 실행할 명령어
			
				※ 클로드 데스크탑에 등록된 MCP 설정 가져오기
					명령어 : ▶ claude mcp add-from-claude-desktop
			
			2. MCP 등록 확인
				- 터미널 : claude mcp list
				- 클로드세션 : /mcp
				
		※ 추천 MCP
		
			1. Context7
				최신 정보 탐색
				
				
			2. Browser MCP
				우리가 사용하는 브라우저(로그인, 세션기록 저장됨)를 사용
				E2E 테스트
				
				▶ claude mcp add browsermcp -- npx @browsermcp/mcp@latest
				▶ Browser MCP 전용 Chrom 확장 프로그램 설치 : https://chromewebstore.google.com/detail/browser-mcp-automate-your/bjfgambnhccakkhmkepdoekmckoijdlc?pli=1&authuser=1
				
				
				* 비슷한 MCP
					Playwright
						- 여러 브라우저 폭 넓게 지원
						
						# Playwright 라이브러리를 설치
						▶ pnpm add -D @playwright/test playwright
						# MCP 등록
						▶ claude mcp add playwright npx -- @playwright/mcp@latest
						
					puppeteer
						- 크롬 브라우저 특화, 속도 빠름
						- E2E 테스트 자동화, 웹 스크래핑 봇, PDF 또는 스크린샷 생성 서비스
						▶ claude mcp add puppeteer npx @modelcontextprotocol/server-puppeteer
					
			3. Jira MCP
				MCP 등록 명령어 : ▶ claude mcp add --transport sse atlassian https://mcp.atlassian.com/v1/sse
			
			4. Observability MCPs(Datadog, Sentry)
			
			
	◆ SuperClaude
	
		WSL 에서 준비 및 설치
	
		■ 사전 준비
		
			터미널(powerShell) > wsl > 
		
			◇ 파이썬 설치
				
				# 설치
				sudo apt update
				sudo apt install -y python3 python3-pip python3-venv

				# 확인
				python3 --version  # Python 3.10.x 이상
				pip3 --version     # pip 23.x.x
				
			◇ 클로드 설치(없다면)

				# Node.js 설치 (필요 시)
				curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
				sudo apt install -y nodejs

				# Claude Code 설치
				npm install -g @anthropics/claude-code

				# 확인
				claude --version
				
			◇ pipx (권장) 설치
			
				# 설치
				sudo apt update
				sudo apt install -y pipx

				# PATH 자동 설정
				pipx ensurepath

				# 터미널 재시작
				source ~/.bashrc

				# 확인
				pipx --version
					
					
		■ SuperClaude 설치
		
			# 1. pipx로 SuperClaude 설치
			pipx install SuperClaude

			# 2. SuperClaude 초기화 (Claude Code 설정)
			SuperClaude install

			# 3. 설치 확인
			SuperClaude doctor